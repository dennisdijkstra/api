import {
    USER_LOADING,
    SET_CURRENT_USER,
    GET_CURRENT_USER,
} from './types';

export const setUserLoading = () => (
    
);

export const setCurrentUser = () => (

);

export const getCurrentUser = () => {

};
